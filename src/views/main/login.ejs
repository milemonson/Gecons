<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="img/icongecons.png" >
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
    <link rel="stylesheet" href="css/styleLogin.css">
    <title>Gecons - Login</title>
</head>

<body>
    <form class="formulario" method="POST" action="/login">
        <h1>Login</h1>
        <div class="contenedor">
            <div class="input-contenedor">
                <i class="fas fa-building icon"></i>
                <input type="text" name="user" id="user" placeholder="Usuario/Nombre de edificio"
                    value="<%= locals.userInput && userInput.user ? userInput.user : '' %>">
            </div>
            <% if(locals.errors && errors.user) { %>
                <div class="alert-label">
                    <p><%= errors.user.msg %></p>
                </div>
            <% } %>

            <div class="input-contenedor">
                <i class="fas fa-key icon"></i>
                <input type="password" name="password" id="password" placeholder="Contraseña">
            </div>
            <% if(locals.errors && errors.password) { %>
                <div class="alert-label">
                    <p><%= errors.password.msg %></p>
                </div>
            <% } %>

            <% if(locals.errors && errors.authenticate) { %>
                <div class="alert-label">
                    <p><%= errors.authenticate.msg %></p>
                </div>
            <% } %>

            <div class="form-row d-flex justify-content-center">
                <div class="form-check w-25 mb-3">
                    <input class="form-check-input" type="checkbox" name="remember-me" id="remember-me" value="remember-me">
                    <label class="form-check-label" for="flexCheckDefault">
                      Recordarme.
                    </label>
                </div>
            </div>

            <button type="submit" class="button">Ingresar</button>
        </div>
    </form>

    <script src="js/validateLogin.js"></script>

</body>

</html>